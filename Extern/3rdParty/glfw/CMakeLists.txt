set (TARGET_NAME glfw)

set(BUILD_SHARED_LIBS ${EVOENGINE_ENABLE_RUNTIME_PACKAGES})

set(GLFW_BUILD_DOCS OFF CACHE BOOL "" FORCE)
set(GLFW_BUILD_TESTS OFF CACHE BOOL "" FORCE)
set(GLFW_BUILD_EXAMPLES OFF CACHE BOOL "" FORCE)
set(GLFW_INSTALL OFF CACHE BOOL "" FORCE)

add_subdirectory(${TARGET_NAME} EXCLUDE_FROM_ALL)

set_property(TARGET ${TARGET_NAME} PROPERTY FOLDER 3rdParty)

set(3RDPARTY_LIBS ${3RDPARTY_LIBS} 
    ${TARGET_NAME}
    PARENT_SCOPE)

set(3RDPARTY_INCLUDES ${3RDPARTY_INCLUDES} ${CMAKE_CURRENT_SOURCE_DIR}/glfw/include PARENT_SCOPE)
if(EVOENGINE_ENABLE_RUNTIME_PACKAGES)
  set(3RDPARTY_RUNTIME_LIBRARIES ${3RDPARTY_RUNTIME_LIBRARIES} $<TARGET_FILE:glfw> PARENT_SCOPE)
  if(MSVC)
    set(3RDPARTY_PDBS ${3RDPARTY_PDBS} $<TARGET_PDB_FILE:glfw> PARENT_SCOPE)
  endif()
endif()
