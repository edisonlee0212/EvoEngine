# DDGI Reference Images

These images are retained stable ray-camera captures for manual or ad-hoc DDGI comparison.

They are not an authoritative baseline bundle and do not define pass/fail policy. Use
`Scripts\compare_reference_render.py` when a fresh capture needs a numeric image delta against one of these references.
